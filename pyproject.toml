[project]
name = "sports-prediction"
version = "0.1.0"
description = "General sports prediction framework with confidence scoring and transparent reasoning"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
